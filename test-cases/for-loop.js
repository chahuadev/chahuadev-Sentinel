// For loop iteration
for (let i = 0; i < 10; i++) {
    console.log(i);
}

// For-of loop
for (const item of array) {
    process(item);
}

// For-in loop
for (const key in object) {
    console.log(key, object[key]);
}
